| id | case | observation | expected | evidence | disposition |
|---|---|---|---|---|---|
| R007 | sop_without_eop_records_syndrome | Real ingress FIFO full during an ingress-open packet sets `STATUS.partial_packet_drop_sticky` and increments `DROPS_REAL`, but `STATUS.drop_mid_packet_sticky`, `ERROR_COUNT_DROP_MID_PACKET`, and `SYNDROME_DROP_MID_PACKET` stay zero. | `tb/DV_ERROR.md` R007 and `doc/RTL_PLAN.md` section 2.5 require the drop-mid-packet sticky, counter, and first-event syndrome for this upstream contract violation. | `tb/sim/R007_sop_without_eop_records_syndrome_test.log`; likely gate is `real_drop_mid_event = real_push_drop & real_source_open` in `rtl/arb_hit_type0_csr.sv`, which misses ingress-open drops while the arbiter is draining the other source. | RTL not patched per ERROR-bucket isolation; test records the mismatch and keeps regression running with zero UVM_ERROR/FATAL. |
