# tb_int BUG_HISTORY (v3_pretest-260511)

Per dv-workflow skill §11..§14. Every real RTL or harness bug found during DV must be recorded here with first-seen testcase, observable symptom, root cause, fix status, and encounterability band.

## Index

| Tag | Severity | Encounterability | First-seen | Status | One-line description |
|---|---|---|---|---|---|

No entries yet. The harness is dispatched to codex1 (per DV_INT_PLAN.md §7); bugs will be recorded here as they are found during BASIC -> EDGE -> ERROR -> PROF bring-up.

## Conventions

- `Severity`: blocker / major / minor.
- `Encounterability`: common / occasional / rare / corner-only / directed-only.
- `Status`: fixed (cite commit hash) / pending (cite gate / blocker) / deferred (cite reason).
- `First-seen`: testcase ID (e.g. `B068`) or regression context (e.g. `bucket_frame BASIC`).
- Format check: `python3 ~/.codex/skills/dv-workflow/scripts/bug_history_format_check.py BUG_HISTORY.md`.

## Detailed entries

(none yet)
